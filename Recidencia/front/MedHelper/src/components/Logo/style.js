import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    logoContainer: {
        marginBottom: 30,
      },
    logo: {
        width: 120,
        height: 120,
    } ,
     welcomeText: {
        fontSize: 24,
        fontWeight: 'bold',
        color: '#fff',
        textAlign: 'center',
      }, 
      subtitleText: {
        fontSize: 16,
        color: '#dcdcdc',
        marginBottom: 40,
        textAlign: 'center',
    }
    
})

export default styles